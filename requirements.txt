
# GMP SOP 교육자료 생성 모듈 - 필수 라이브러리 (GPT-4)

# 핵심 라이브러리
openai>=1.12.0
python-docx>=1.1.0
python-dotenv>=1.0.0

# Flask 백엔드 (Flask 사용 시)
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# FastAPI 백엔드 (FastAPI 사용 시)
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# 유틸리티
typing-extensions>=4.9.0